# PersonalMentor — Python Dependencies

# Core
pyyaml>=6.0
requests>=2.31
beautifulsoup4>=4.12
feedparser>=6.0

# SSL — use OS certificate store (fixes corporate proxies on Windows)
truststore>=0.9

# CV parsing (onboarding)
pypdf>=4.0
python-docx>=1.1

# Templating (Jinja2-cv skill)
docxtpl>=0.16

# Google AI (German sentence generation)
google-genai>=1.0

# Environment variable loading (cross-platform .env support)
python-dotenv>=1.0
